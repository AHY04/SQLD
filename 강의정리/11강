1.집합연산자
-여러 SQL결과를 연결해 하나의 형태로 결합하는 문법
-UNION: 중복을 제거하고 정렬을 수행하며 합집합 처리함
-UNION ALL: 중복을 포함하고 정렬을 수행하지 않으며 합집합 처리함
-INTERSECT: 중복을 제거하고 교집합 처리함
-MINUS: 중복을 제거하고 차집합 처리함
-컬럼 여러 개 사용도 가능하나, 위 아래 컬럼개수와 형식이 다르면 에러가 발생함
-집합연산자를 쓸 때는 ORDER BY를 항상 마지막에 써야함
-위 아래 컬럼 이름이 다를 경우 위 컬럼의 이름을 따름(데이터유형, 컬럼개수는 동일해야함)
-집합연산자가 여러개일 경우에는 위에서 아래로 순서대로 풀이하면 됨(중복제거 주의)

2.그룹함수(group by를 여러번 사용해서도 표현 가능)
-ROLLUP: 소계와 합계를 한번에 추출 가능
-CUBE: 입력된 컬럼의 모든 경우의 수를 집계
-GROUPING SETS: 입력한 컬럼 그대로 집계처리

3.윈도우함수
-테이블의 행과 행간의 관계를 정의, 비교, 연산할 때 쓸 수 있는 함수 종류
-윈도우 함수 문법: WINDOW_FUNCTION(매개변수) OVER([PARTITION BY 컬럼][ORDER BY 절][WINDOWING 절])
-순위함수
  1) RANK: 동 순위가 있으면 순위를 건너뛰고 부여
  2) DENSE_RANK: 동 순위가 있어도 순위를 건너뛰지 않음
  3) ROW_NUMBER: 동 순위든 아니든 고유한 번호를 부여
-LEAD함수: 현재행을 기준으로 N번째 이후 행의 컬럼값을 가져옴
-LAG함수: 현재행을 기준으로 N번째 이전 행의 컬럼값을 가져옴
