1.PIVOT과 UNPIVOT
-PIVOT: 행 -> 열로 바꾸기
-해석방법
  1) FROM뒤에 인라인뷰처럼 사용하려는 컬럼만 명시
  2) PIVOT뒤의 FOR뒤에 행을 열로 바꾸려는 컬럼과 값을 입력
  3) PIVOT과 FOR 사이에 집계처리를 할 컬럼을 입력
  4) FROM안에 있는 SELECT뒤에 사용안된 대상들을 그대로 입력
-UNPIVOT: 열 -> 행으로 바꾸기

2.정규표현식
-데이터에서 특정규칙을 가지는 문자열을 찾거나 처리하는데 도움이 되는 고급 쿼리 방법
-REGEXP_LIKE: 패턴이 특정 형식을 만족하는지 확인할 때 사용 가능, 정규표현식이 매칭되면 TRUE, 아니면 FALSE를 반환
-REGEXP_REPLACE: 패턴에서 특정 문자열을 치환할 때 사용 가능
-REGEXP_SUBSTR: 패턴에서 특정 문자열을 추출할 때 사용 가능, 첫번째 인수와 두번째 표현식이 일치하는 문자열을 반환
-REGEXP_COUNT: 패턴에서 특정 문자열의 반복개수를 확인할 때 사용 가능, 패턴 일치하는 개수 확인
-REGEXP_INSTR: 패턴에서 특정 문자열이 존재하는지 확인할 때 사용 가능, 일치하는 패턴의 시작 위치를 반환
-정규표현식에서 사용하는 와일드카드
  1) .: 모든 문자와 일치(모든문자, 문자1개와 일치)
  2) |: 대체 문자를 구분
  3) \: 특수문자를 이스케이프 처리(특수문자->일반문자)
  4) ^(캐럿): 문자열의 시작
  5) $: 문자열의 끝
  6) \d: 숫자 1개
  7) \w: 문자 1개
-수량사(탐욕적)
  1) ?: 0~1회 일치
  2) *: 0~무제한 일치
  3) +: 1~무제한 일치
  4) (m): m회 일치
  5) (m, ): 최소m회 일치
  6) (m,n): 최소m ~ 최대n회 일치
-표현식: ()안의 표현식을 하나의 단어로 취급(예: (abc))
-PERL 문자 클래스: [0-9] [a-z] [A-Z] [a-zA-Z] 등
-POSIX 문자 클래스: [:digit:] [:lower:] [:alpha:]
-PERL 정규표현식연산자(비탐욕적)
  1) ??: 0~1회 일치
  2) *?: 0~무제한 일치
  3) +?: 1~무제한 일치
  4) (m)?: m회 일치
  5) (m, )?: 최소m회 일치
  6) (m,n)?: 최소m ~ 최대n회 일치
-
